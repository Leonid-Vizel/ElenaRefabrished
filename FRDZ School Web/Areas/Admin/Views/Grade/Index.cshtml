@using FRDZSchool.Utility;
@using FRDZSchool.Models.DatabaseModels;
@model IEnumerable<Grade>
@{
    ViewData["Title"] = "Классы";
}

<div class="container pt-4">
    <h1 class="text-primary text-center display-5 tracking-in-expand">Список классов</h1>
    <div class="table-responsive pt-3">
        <div class="table-wrapper" style="width: 99%">
            <table id="gradeTable" class="table text-nowrap table-hover mb-0 cell-centered">
                <thead class="noselect">
                    <tr class="table-dark cell-centered">
                        <th scope="col">@Html.DisplayNameForInnerType((Grade g)=>g.Number)</th>
                        <th scope="col">@Html.DisplayNameForInnerType((Grade g)=>g.Specialization)</th>
                        <th scope="col">
                            <a asp-controller="Grade" asp-action="Add" class="btn btn-success w-100"><i class="bi bi-plus-square big-icon"></i> Добавить</a>
                        </th>
                    </tr>
                </thead>
                <tbody>
                   @* @foreach (var obj in Model)
                    {
                        <tr class="cell-centered">
                            <td scope="row">@obj.Id</td>
                            <td>@obj.Number "@obj.Litera"</td>
                            <td>@obj.Specialization</td>
                            <td class="sticky-col right-col" style="background-color: rgba(235, 235, 235, 0.9)">
                                <div class="row g-0">
                                    <div class="col-6">
                                        <a asp-controller="Grade" asp-action="Edit" asp-route-Id="@obj.Id" class="btn btn-outline-warning"><i class="bi bi-pencil-square big-icon"></i> Изменить</a>
                                    </div>
                                    <div class="col-6">
                                        <a asp-controller="Grade" asp-action="Delete" asp-route-Id="@obj.Id" class="btn btn-outline-danger"><i class="bi bi-x-square big-icon"></i> Удалить</a>
                                    </div>
                                </div>
                            </td>
                        </tr>
                    }*@
                </tbody>
            </table>
        </div>
    </div>
</div>

@section Scripts
{
    <script src="~/js/grade.js" asp-append-version="true"></script>
}